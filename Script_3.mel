// print how may objects and node are in the selection
string $sel[] = `ls -sl`;
string $nodeSel [] = `ls -sl -type node`;
// print size of the selection
print ("You have selected " + size($sel) + " object and " + size($nodeSel) + " node\n");